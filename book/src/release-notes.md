<!-- This page is generated from the repository-root CHANGELOG.md via mdBook
     {{#include}}. Do NOT edit release notes here: edit ../../CHANGELOG.md
     (Keep a Changelog + SemVer) and the change flows through automatically, so
     the book can never drift from the actual releases. -->

{{#include ../../CHANGELOG.md}}
