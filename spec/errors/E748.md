+++
code = 'E748'
name = 'Leading zero in bullet timestamp'

[[example]]
level = 'tier'
claim = 'violates'
chat = """
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Media:	session, audio
*CHI:	hey . \u0015012_200\u0015
@Comment:	ERROR: start time 012 has a leading zero
@End
"""

[[example]]
level = 'tier'
claim = 'violates'
chat = """
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Media:	session, audio
*CHI:	hey . \u0015100_012\u0015
@Comment:	ERROR: end time 012 has a leading zero
@End
"""
[[example]]
title = 'A bare zero timestamp is legal'
level = 'tier'
claim = 'legal'
chat = """
@UTF8
@Begin
@Languages:\teng
@Participants:\tCHI Target_Child
@ID:\teng|corpus|CHI|||||Target_Child|||
@Media:\tsession, audio
*CHI:\they . \u00150_200\u0015
@End
"""

[[example]]
title = 'Both timestamp components have leading zeros'
level = 'tier'
claim = 'violates'
notes = 'Each component is independently malformed; diagnostics retain start-before-end order while the numeric bullet is preserved.'
chat = """
@UTF8
@Begin
@Languages:\teng
@Participants:\tCHI Target_Child
@ID:\teng|corpus|CHI|||||Target_Child|||
@Media:\tsession, audio
*CHI:\they . \u0015012_0200\u0015
@End
"""

[[example]]
title = 'End overflow prevents leading-zero diagnostics for the pair'
level = 'tier'
claim = { subsumed_by = 'E360' }
notes = 'Retain start 012 from the preceding control and change only the end count to one above u64 maximum. Numeric pair admission fails before spelling diagnostics are emitted.'
chat = """
@UTF8
@Begin
@Languages:\teng
@Participants:\tCHI Target_Child
@ID:\teng|corpus|CHI|||||Target_Child|||
@Media:\tsession, audio
*CHI:\they . \u0015012_18446744073709551616\u0015
@End
"""
+++

**Status:** Current
**Last updated:** 2026-09-23 23:53 EDT

## Description

A media bullet timestamp is written with a leading zero before another
digit (for example `012_200`). CHAT bullet times are plain
millisecond integers; a leading zero is an illegal time representation
(CLAN CHECK error 90, `check_getMediaTagInfo` res 3). A bare `0`
timestamp (for example `0_200`) is legal: the rule fires only
when a `0` is followed by another digit.

## Expected Behavior

When both components have leading zeros, report the start spelling before the
end spelling. Numeric pair admission precedes these diagnostics: if either
component overflows, E360 rejects the pair without emitting E748 for the other
component. This is diagnostic precedence, not acceptance of its spelling.

- **Parser**: Reports E748 on the bullet whose timestamp carries the
  leading zero. The bullet's numeric value still parses (recovery is
  preserved; the diagnostic makes the file invalid).
- **Validator**: No separate validation-layer check; the raw digit text
  exists only in the source, so the parser is the only layer that can
  see the leading zero.

## CHAT Rule

Bullet times are integer milliseconds. CLAN CHECK rejects a time
component matching `0[0-9]` as an "Illegal time representation inside
a bullet." (code 90). Parity entry:
`crates/talkbank-parser-tests/tests/check_parity/manifest.json`
CHECK 90.
