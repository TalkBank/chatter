+++
code = 'E708'
name = 'Malformed grammar relation on %gra tier'
status_note = "Reachable under both parsers, and the note that stood here said otherwise until 2026-09-08. It was right that `gra_index: /[0-9]+/` keeps NON-NUMERIC indices out, and wrong to conclude from that that no input reaches the code: a numeric index too large for the model's integer type satisfies the regex, builds a well-formed relation node, and fails when the digits are read as a number. That is the second example below. The shape of the mistake is worth more than the fix: a note reporting which shapes were TRIED had been read as a proof that none can work."

[[example]]
title = 'A relation with no index, on a `%gra` tier with no `%mor`'
level = 'tier'
source = 'E7xx_tier_parsing/E709_gra_missing_index.cha'
claim = { subsumed_by = 'E604' }
notes = '''
This claimed `violates` E708 and does not emit it. Nothing checked that while
E708's registry status was `not_implemented`, because the status excuses an
example from every gate; the first measurement after the status was corrected
reported the spec unmet.

What this input does: the `%gra` tier has no `%mor` beside it, so E604 rejects
the file before any relation is parsed.

What the input WITH a `%mor` does, measured the same day: `%mor:` + tab +
`co|hello n|world .` beside `%gra:` + tab + `|2|SUBJ 2|0|ROOT 3|2|PUNCT` gives
E720, a `%mor`/`%gra` count mismatch, not E708. ALL THREE relations are
dropped, not just the malformed one: the reported mismatch is 3 alignable
`%mor` items against 0 `%gra` relations. So one bad relation costs the whole
tier, and the diagnostic a reader sees names the count rather than the
relation. Whether that is right is an open question
for whoever owns `%gra`; it is recorded here because a reader arriving at E708
with a missing index will otherwise look for a code that does not come.
'''
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	hello world .
%gra:	|2|SUBJ 2|0|ROOT
@End
'''
[[example]]
title = 'An index too large to be an index'
level = 'tier'
claim = 'violates'
notes = '''
The `status_note` said the grammar's `gra_index: /[0-9]+/` makes this
unreachable, having considered only NON-NUMERIC indices. A numeric index
that overflows the model's integer type satisfies the regex, builds a
well-formed relation node, and then fails to parse into a number, which
is exactly the case `relation.rs` was written for.
'''
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	the dog .
%mor:	det|the n|dog .
%gra:	99999999999999999999999|2|DET 2|0|ROOT 3|2|PUNCT
@End
'''

+++

## Description

A grammar relation on the `%gra` tier is malformed, missing an index, head,
or relation label, or containing non-integer values where integers are expected.
The `%gra` tier format is `index|head|RELATION` for each word.

## Expected Behavior

The parser should report E708 when a `%gra` relation is missing its index,
head, or relation label. The check exists in
`crates/talkbank-parser/src/parser/tier_parsers/gra/relation.rs`.

**Trigger condition, as measured**: a `%gra` relation whose index is numeric
(the grammar admits nothing else) and too large to read as an integer.

The three conditions listed here before were the rule's INTENT, and two of
them do not reach it: a missing index gives E720, a count mismatch, because
the whole tier's relations are dropped; a missing relation label gives E316.
Both are recorded on the first example below.

## CHAT Rule

See CHAT manual on the `%gra` tier. Each relation must follow the format
`index|head|RELATION` where index and head are integers. The CHAT manual is
available at: https://talkbank.org/0info/manuals/CHAT.pdf

## Notes

- Validation logic exists in `gra/relation.rs` with multiple emission points
- The grammar requires `gra_index` and `gra_head` to match `[0-9]+`, so
  non-numeric indices never create a `gra_relation` CST node
- The example has `|2|SUBJ` (missing index / leading pipe) which the grammar
  does not parse as a GRA relation node at all; it becomes an ERROR node
- The code IS emitted in the codebase for malformed GRA relations, but the
  tree-sitter grammar's strict regex pre-validates the content
