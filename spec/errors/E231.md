+++
code = 'E231'
name = 'Unbalanced shortening parenthesis'

[[example]]
level = 'word'
source = 'E2xx_word_errors/E231_unbalanced_shortening.cha'
claim = { subsumed_by = 'E316' }
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	hel(o .
@End
'''
+++

## Description

A word's shortening markers, `(` `)` around an omitted syllable/segment (e.g.
`(be)cause` for an elided "be"), must nest and balance like ordinary
parentheses: every `)` must be preceded by a matching unclosed `(`, and every
`(` must eventually be closed. The check is stack-based (implemented in
`crates/talkbank-model/src/validation/word/structure.rs::check_shortening_balance`),
not a bare count, so it also catches a `)` appearing before any `(` (reported
immediately, at the point of the stray close) in addition to a trailing
unclosed `(` (reported once, after the whole word is scanned).

**This example does not currently reach that check**: `hel(o` (missing the
closing `)`) is malformed enough that the parser itself cannot build a valid
word from it and reports the generic unparsable-content code (E316) before
model-level word validation ever runs. E231 is real, implemented, and
reachable, just not by this specific input shape.

## Expected Behavior

For an input where the parser DOES successfully build a `Word` value
containing an unbalanced parenthesis run (rather than failing to parse
entirely), model validation reports E231 at the point of the imbalance: an
early `)` is flagged immediately, a trailing unclosed `(` is flagged once
after the scan.

## CHAT Rule

<https://talkbank.org/0info/manuals/CHAT.html#Word> documents `(text)` as the
shortening/omission marker; it must be a balanced, nestable pair around the
omitted material.

## Notes

- **Needs a `violates` example that actually reaches this code**: something
  like a word where the parser recovers past the malformed parenthesis and
  still builds a `Word` (e.g. an extra unmatched `)` appearing mid-word after
  otherwise-valid content) would demonstrate the real check rather than only
  the parser-level subsumption. This is exactly the kind of `subsumed_by`
  entry the design doc calls a genuine parser/spec-authoring gap, not a
  documented-permanent case like E208's; worth revisiting once a suitable
  fixture is found.
