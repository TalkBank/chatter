+++
code = 'E503'
name = 'Missing required @UTF8 header'

[[example]]
level = 'header'
title = 'File without @UTF8'
claim = 'violates'
chat = '''
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	hello world .
@End
'''
[[example]]
level = 'header'
title = 'Encoding declared, document retained'
claim = 'legal'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	hello world .
@End
'''
+++

**Status:** Current
**Last updated:** 2026-09-06 15:18 EDT

## Description

Every valid CHAT file must begin with an `@UTF8` header as its first line. This error indicates the file is missing `@UTF8`, which means the file's character encoding is unspecified. All modern CHAT files are expected to be UTF-8 encoded.

## Expected Behavior

Parsing must retain the present headers and utterance. Validation reports
E503 at the **end of the file** and suggests adding `@UTF8` as the first
line. It must not claim that the present `@Begin`, `@Languages`,
`@Participants`, or `@End` headers are missing. Adding the declaration to
the same document removes the error.

## CHAT Rule

Every CHAT file must begin with `@UTF8` on the first line, followed by `@Begin`. See CHAT manual section on file structure: https://talkbank.org/0info/manuals/CHAT.pdf

## Notes

- The grammar intentionally accepts files without `@UTF8` (parse-lenient), and this validation rule closes the gap (validate-strict)
- This fills the E503 slot in the E5xx sequence between E502 (MissingEndHeader) and E504 (MissingRequiredHeader)

CLAN CHECK (07-Aug-2026), run on the generated missing-declaration fixture,
reports CHECK (69), "The UTF8 header is missing." The declaration remains
required for validity; parser recovery must not discard the rest of the file.
