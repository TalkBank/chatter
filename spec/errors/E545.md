+++
code = 'E545'
name = '@Birth of date does not match a legal CHAT date pattern'
status_note = 'New validation added 2026-04-21 to close the `@Birth of` gap discovered during the depfile.cut conformance audit. Reuses the same date-format logic as `@Date` (E518) since depfile.cut gives both headers the same `@d<dd-lll-yyyy>` template.'

[[example]]
level = 'header'
title = '(lowercase month)'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|2;06.||||Target_Child|||
@Birth of CHI:	15-jan-2024
*CHI:	hello .
@End
'''

[[example]]
level = 'header'
title = '(two-digit year)'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|2;06.||||Target_Child|||
@Birth of CHI:	15-JAN-24
*CHI:	hello .
@End
'''

[[example]]
level = 'header'
title = '(numeric month)'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|2;06.||||Target_Child|||
@Birth of CHI:	15-01-2024
*CHI:	hello .
@End
'''

[[example]]
level = 'header'
title = '(free text)'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|2;06.||||Target_Child|||
@Birth of CHI:	last Tuesday
*CHI:	hello .
@End
'''
[[example]]
level = 'header'
title = 'Canonical birth date control for digit substitutions'
claim = 'legal'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Birth of CHI:	01-JAN-2024
*CHI:	hello .
@End
'''

[[example]]
level = 'header'
title = 'Birth day cannot use a positive integer sign'
claim = 'violates'
notes = 'Replace only the first day digit in the preceding control with +.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Birth of CHI:	+1-JAN-2024
*CHI:	hello .
@End
'''

[[example]]
level = 'header'
title = 'Birth year cannot use a positive integer sign'
claim = 'violates'
notes = 'Replace only the first year digit in the canonical control with +.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Birth of CHI:	01-JAN-+024
*CHI:	hello .
@End
'''
[[example]]
level = 'header'
title = 'An empty birth date preserves the header without inventing a date'
claim = 'legal'
notes = 'Deletes only the value from the canonical control, retaining the header separator. CHECK and Chatter accept this unknown-date boundary. The parsed model retains a Birth header with empty date text; it does not drop the header or invent a calendar value.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Birth of CHI:	
*CHI:	hello .
@End
'''
+++

## Description

When nonempty, an `@Birth of <CODE>` date must match CLAN's
authoritative `depfile.cut` date template:

```
@Birth of #:	@d<dd-lll-yyyy>
```

That is, the value must be `DD-MMM-YYYY`:

- `DD`: two ASCII digits, 01-31, without a sign
- `MMM`: three-letter uppercase month abbreviation (JAN..DEC)
- `YYYY`: four ASCII digits, without a sign

Anything else, lowercase month, two-digit year, numeric month,
free text, is rejected by CLAN CHECK, and Rust chatter must match.

Rust chatter historically did not validate `@Birth of` at all; the
date was stored as a raw string on `Participant.birth_date`
without any format check. This was a depfile.cut conformance gap.

## Expected Behavior

- **Parser**: retains the birth header for a supplied date value or the
  tab-only unknown-date boundary demonstrated by example 8.
- **Validator**: reports E545 at the header span when the value
  does not match the canonical `DD-MMM-YYYY` form. The check
  reuses the same code as `@Date` (`E518`), which already
  enforces the day/month/year rules granularly.
- **Empty birth date**: remains empty and does not emit E545. CHECK
  21-Sep-2026 accepted the canonical and empty controls on 2026-09-23.

## CHAT Rule

CLAN `depfile.cut`:

```
@Birth of #:	@d<dd-lll-yyyy>
```

CHAT manual: <https://talkbank.org/0info/manuals/CHAT.html>

An empty birth date may represent an unknown date. Preserve the header and
its empty value rather than supplying a calendar date or treating this case
as a malformed nonempty date. The finite empty-value control retains its tab
separator and roundtrips byte-for-byte.

## Notes

- Authoritative source: `clan-info/lib/depfile.cut:23`.
- Severity: `Error` (depfile rejections are errors in CLAN CHECK).
- Implementation reuses `check_date_format` from `metadata.rs`;
  the difference from `@Date` is only the error code emitted.
