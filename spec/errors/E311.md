+++
code = 'E311'
name = 'Unclosed replacement bracket'

[[example]]
level = 'utterance'
source = 'E3xx_main_tier_errors/E311_failed_parse_utterance.cha'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	[: unclosed replacement [* error] .
@End
'''

[[example]]
level = 'utterance'
source = 'E3xx_main_tier_errors/E311_unclosed_replacement_mid_utterance.cha'
claim = 'violates'
notes = '''
Example 1 puts the unclosed `[:` at the start of the utterance; this one puts
it after a word. The two reach the classifier by different routes, and only the
utterance-initial one was covered, so a change that preserved Example 1 while
silently degrading this case to E316 (the generic "unparsable content"
catch-all) passed every gate. One example per code is coverage of the code, not
of the rule.
'''
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	hello [: world .
@End
'''

[[example]]
title = 'A closed replacement'
level = 'utterance'
claim = 'legal'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	hello [: world] .
@End
'''
+++

**Status:** Current
**Last updated:** 2026-09-03 17:20 EDT

## Description

A replacement scope `[: ...]` is opened and never closed, so the words that
were meant as the replacement target run on into the rest of the utterance.

## Expected Behavior

Tree-sitter recovery leaves the material from `[:` onward as an ERROR node;
the parser locates the unclosed opener inside it and reports E311 over the
bracket, whether the opener is utterance-initial or follows a word. A closed
`[: form]` after a word parses as a replacement and is not reported.

**Trigger**: See examples above

## CHAT Rule

The CHAT manual's "Replacement" scoped symbol is `[: text]`, attached to the
word it replaces and closed before the next word:
<https://talkbank.org/0info/manuals/CHAT.html#Replacement_Scope>. Scoped
symbols in general are square-bracketed and balanced
(<https://talkbank.org/0info/manuals/CHAT.html#Scoped_Symbols>).

## Notes

- The registry variant is the historical `UnexpectedNode`; the message and
  both examples are about the unclosed replacement bracket, which is the one
  thing this code reports today.
- Marked unreachable until 2026-08-11 on the strength of a stale note; the
  parser had outgrown the note and the code was live all along.
