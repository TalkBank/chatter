+++
code = 'E711'
name = 'Mor empty content'
status_note = "Reachable under both parsers, and the note that stood here said otherwise until 2026-09-08. Both halves of its reasoning are true: an empty stem (`v|`) and an empty POS (`|want`) are caught structurally before the model is built. The conclusion does not follow, because a `%mor` word has more parts than those two, and a trailing `=` leaves the FEATURE empty on a word the grammar accepts whole. That is the third example below."

[[example]]
level = 'tier'
claim = { subsumed_by = 'E702' }
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|test|CHI||female|||Target_Child|||
*CHI:	want cookie .
%mor:	v| n|cookie .
@End
'''

[[example]]
level = 'tier'
claim = { subsumed_by = 'E760' }
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|test|CHI||female|||Target_Child|||
*CHI:	want cookie .
%mor:	|want n|cookie .
@End
'''
[[example]]
title = 'A feature with no content'
level = 'tier'
claim = 'violates'
notes = '''
The `status_note` said an empty stem (`v|`) and an empty POS (`|want`)
are both caught structurally before the model is built, and concluded the
code was unreachable. Both halves are true and the conclusion does not
follow: a MOR word has several parts that can be empty, and a trailing
`=` leaves the FEATURE empty on a word the grammar accepts whole.
'''
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	dog .
%mor:	n|dog-Num= .
@End
'''

+++

## Description

A %mor word has an empty stem, POS category, prefix, suffix, or FEATURE
VALUE. Every morphosyntax item on the %mor tier must have a non-empty POS
category and a non-empty stem at minimum, and every feature it carries must
have a value.

The feature value was missing from this sentence until 2026-09-08, and it is
the only one of the five that any CHAT input reaches: an empty stem and an
empty POS are both caught structurally before the model is built, which is
what the old `status_note` correctly observed before drawing the wrong
conclusion from it. `n|dog-Num=` is a word with a non-empty POS and a
non-empty stem, and `MorFeature::is_empty` reports its keyed feature as empty
because the value after `=` is.

## Expected Behavior

The parser should successfully parse this CHAT file, but validation should
report an error. Every %mor item must contain a non-empty POS category and a
non-empty stem, and every feature it carries must have a value; the third
example is the feature case, which is the one reachable from CHAT.

## CHAT Rule

The %mor tier format is `POS|stem` with optional prefix, suffix, and fusion
markers. Neither POS nor stem may be empty. See the CHAT manual:
https://talkbank.org/0info/manuals/CHAT.pdf

## Notes

- E711 validation logic exists and is tested at the model layer, but the
  tree-sitter grammar's structural requirements prevent empty POS/lemma from
  reaching the model construction phase.
- Related codes: E706 (count mismatch), E708 (malformed grammar relation).
