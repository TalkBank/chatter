+++
code = 'E352'
name = 'MissingQuoteEnd'

[[example]]
level = 'utterance'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|test|CHI||male|||Target_Child|||
*CHI:	I want a cookie .
*CHI:	+, actually ice cream .
@End
'''
[[example]]
title = 'A resumed turn can be interrupted again before its next completion'
level = 'file'
claim = 'legal'
notes = 'Strict-linker lifecycle control: each self-completion consumes one pending interruption; its own interruption terminator then supplies the next one.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child, MOT Mother
@ID:	eng|corpus|CHI|||||Target_Child|||
@ID:	eng|corpus|MOT|||||Mother|||
*CHI:	I want +/.
*MOT:	what ?
*CHI:	+, some +/.
*CHI:	+, cookies .
*CHI:	please .
@End
'''

[[example]]
title = 'An already consumed interruption cannot satisfy another self-completion'
level = 'file'
claim = 'violates'
notes = 'Single-linker insertion into the final turn of the control. Both earlier interruptions have already been consumed, so the last self-completion has no pending match.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child, MOT Mother
@ID:	eng|corpus|CHI|||||Target_Child|||
@ID:	eng|corpus|MOT|||||Mother|||
*CHI:	I want +/.
*MOT:	what ?
*CHI:	+, some +/.
*CHI:	+, cookies .
*CHI:	+, please .
@End
'''
+++

## Description

A self-completion linker (`+,`) was used and there IS a prior utterance
from the same speaker, but that prior utterance did not end with a `+/.`
(interruption) terminator, or all prior interruptions have already been
completed. Under the optional strict-linker policy, each interruption can
satisfy only one self-completion.

## Expected Behavior

Validation should report E352. The `+,` linker signals self-completion,
which requires the prior same-speaker utterance to have been interrupted
(`+/.`). But the prior CHI utterance ends with `.` (a normal terminator).

## CHAT Rule

The `+,` linker pairs with the `+/.` terminator. The prior same-speaker
utterance must end with `+/.` to indicate it was interrupted, and the
`+,` utterance completes it.

A turn carrying both `+,` and `+/.` completes a prior interruption before
introducing its own new interruption. The final paired examples exercise this
order and the refusal to reuse a consumed interruption under the existing
opt-in policy; they do not enable strict-linker checks by default.

CHECK 21-Sep-2026 accepted both lifecycle specimens on 2026-09-23. They
therefore pin Chatter's optional strict policy, not a CHECK rejection.
