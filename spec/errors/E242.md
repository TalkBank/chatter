+++
code = 'E242'
name = 'Unbalanced quotation delimiters'

[[example]]
level = 'utterance'
source = 'error_corpus/validation_errors/E242_unbalanced_quotation.cha'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Comment:	ERROR: Quotation delimiters must be balanced
@Comment:	Invalid: opening U+201C has no closing U+201D
*CHI:	“hello .
@End
'''

[[example]]
title = 'A closing quotation delimiter without an opening delimiter is invalid'
level = 'utterance'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	hello” .
@End
'''

[[example]]
title = 'Retracing quoted material is legal'
level = 'utterance'
claim = 'legal'
notes = '''
Reported by the CHAT maintainer against a real transcript, 2026-08-26. The
four curly quotes here are two MATCHED pairs, so E242 has nothing to report;
chatter emitted it anyway because the region failed to parse and the ERROR
node happened to contain a curly quote.

Real CLAN CHECK accepts this line, both as this fixture and inside the
reporting transcript. The construct also plainly makes sense: the speaker
quoted a word, retraced, and asked about it.

CORRECTED 2026-08-26, the same day: an earlier version of this note said
"53 files under the data repos use a quotation followed directly by a scoped
annotation". That counted a TEXT PATTERN, not affected files. Every one of the
53 matches is a CA annotation (“I adjust” [^ nom]), which reduces elsewhere
and parsed cleanly before the fix: the pre-fix binary rejects NONE of them.
The published corpora contain ZERO instances of the broken construct, which
arrives with incoming data instead. The fix is still right and CHECK still
accepts it; the impact figure was measured with the wrong instrument.

The cause is that `content_item` offered `quotation` as a bare alternative
while only `group_with_annotations` could carry a `base_annotations`, so
`“plant” [//]` had no production to reduce into.
'''
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	TEA Teacher
@ID:	eng|corpus|TEA|||||Teacher|||
*TEA:	“plant” [//] what is “plant” ?
@End
'''
+++

**Status:** Current
**Last updated:** 2026-09-01 07:07 EDT

## Description

An opening curly double quote (`“`, U+201C) starts a short quotation stretch
inside an utterance. It must be paired with a closing curly double quote (`”`,
U+201D). E242 reports a missing or out-of-order partner within that utterance;
cross-utterance quotation linkers are checked separately.

## Expected Behavior

The parser recovers the malformed quotation and reports E242 at its unpaired
delimiter. A quotation with one opening and one closing delimiter does not
trigger E242, including when the complete quoted region carries a retrace.

**Trigger**: See example above

## CHAT Rule

The CHAT manual's “Quotation” section defines U+201C as the begin delimiter and
U+201D as the end delimiter for a short quotation stretch inside an utterance:
<https://talkbank.org/0info/manuals/CHAT.html#Quotation>. A quotation stretch
therefore requires the ordered pair rather than an unpaired delimiter.

## Notes

- E242 is the intra-utterance balance diagnostic. Cross-utterance `+"`
  quotation continuation and termination use their own linker checks.
- The legal retrace example is an attested parser regression boundary: the
  quotation is balanced, and the annotation belongs to the whole quotation.
