+++
code = 'E203'
name = 'Invalid form type marker'

[[example]]
level = 'word'
source = 'E2xx_word_errors/E203_invalid_form_marker.cha'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	dog@b@c .
@End
'''

# The case the 2026-08-27 ruling actually decided: a DECLARED form marker
# followed by a DECLARED language suffix, each of which is valid alone. The
# `dog@b@c` example above was already invalid before the ruling, so it pins
# nothing about it. This one also pins the TOKENIZATION, which is what went
# wrong twice in one day: a pattern too general swallowed the language suffix,
# and the narrowed replacement dropped `word@k@st` to a generic E316.
[[example]]
level = 'word'
source = 'E2xx_word_errors/E203_two_at_suffixes.cha'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng, spa
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	the gumma@c@s:spa fell .
@End
'''

[[example]]
title = 'A form marker cannot be embedded inside a shortening'
level = 'word'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	c(a@b)t .
@End
'''

[[example]]
title = 'Removing the embedded marker leaves a legal shortening'
level = 'word'
claim = 'legal'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	c(a)t .
@End
'''

[[example]]
title = 'Moving the form marker to the word suffix is legal'
level = 'word'
claim = 'legal'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	c(a)t@b .
@End
'''
[[example]]
title = 'Two form markers embedded inside one shortening'
level = 'word'
claim = 'violates'
notes = 'Derived from the legal c(a)t control by inserting @b@c inside its shortening. Recovery must not bypass the one-marker restriction.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	c(a@b@c)t .
@End
'''

[[example]]
title = 'Embedded form marker combined with a valid outer suffix'
level = 'word'
claim = 'violates'
notes = 'Derived from the legal c(a)t@b control by inserting @c inside the shortening. A valid outer suffix cannot legalize the embedded marker.'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
*CHI:	c(a@c)t@b .
@End
'''
+++

## Description

Word contains an invalid or undeclared `@` form type marker, OR carries more
than one `@` suffix.

An `@` marker inside a shortening, as in `c(a@b)t`, is not a word suffix.
That malformed placement is rejected by both Chatter and CHECK (97, with
147 also possible), unlike the intentionally stricter suffix-stacking rule
below. Removing the embedded marker or moving it after the complete word
provides the paired legal controls.

**A word may carry at most ONE `@` suffix.** Ruled by the maintainer on
2026-08-27, asked because CLAN CHECK accepts multiple suffixes and chatter does
not: "Multiple suffixes might make logical sense, but it is computationally
messy. So, let's disallow that." He added that the combination is usually
incoherent anyway: calling a child-invented form a Spanish word "is wrong in
the first place".

So `dog@b@c` is invalid for stacking two form markers, and `gumma@c@s:spa` is
invalid too, even though the child-invented marker `@c` and the language suffix
`@s:spa` are each perfectly good on their own. Main-tier words carrying two `@`
runs number ZERO across the ~106,000 kept files; the shape occurs only inside
`%com` and `%exp` free text.

**This is a DOCUMENTED DIVERGENCE from CLAN CHECK**, which passes these files.
chatter is deliberately stricter, on the ruling above.

## Expected Behavior

The appropriate error should be reported; which stage catches it is observed in the snapshot, not declared.

## CHAT Rule

The CHAT manual documents `@` word suffixes (form types, e.g. `@b` babbling,
`@c` child-invented form, `@s:xxx` language-tagged) individually; it does not
document stacking two of them on one word, and nothing in the manual's word
grammar admits a second `@` run. See
<https://talkbank.org/0info/manuals/CHAT.html#Word> for the form-type suffix
list. Absent a documented multi-suffix grammar, chatter treats a second `@`
run as invalid rather than silently accepting whichever combination happens to
parse, per the 2026-08-27 maintainer ruling recorded above.

## Notes

- The first two examples pin two different failure shapes under the same
  code: a single word carrying two DECLARED form markers (`dog@b@c`), and a
  word carrying one declared form marker plus a declared language suffix
  (`gumma@c@s:spa`): both individually valid, invalid only in combination.
- This is a documented divergence from CLAN CHECK (see Description); do not
  treat CHECK's silence on files like these as evidence chatter is wrong.
- Examples 6 and 7 mutate the legal shortening controls in examples 4 and 5.
  Both produce parse E316 and validation E203, with divergent recovered
  serialization. CHECK (21-Sep-2026) accepts both controls and rejects both
  embedded-marker mutations with 97 and 147 (97 twice for example 6).
  Unlike a repeated outer suffix, which the parser records as an undeclared
  form, these source shapes reach the model's repeated-marker check. Keep
  that recovery check until producer invariants cover embedded markers too.
