+++
code = 'E207'
name = 'Unknown scoped annotation marker'

[[example]]
level = 'word'
source = 'E2xx_word_errors/E207_multiple_form_types.cha'
claim = 'violates'
chat = '''
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Child
@ID:	eng|corpus|CHI|||||Child|||
*CHI:	hello [@ xyz] world .
@End
'''
+++

## Description

A `[...]` scoped annotation whose marker CLAN/chatter does not recognize as
one of the defined scoped-annotation types (e.g. `[: text]` replacement,
`[/]` retracing, `[* code]` error coding, and the rest of the closed set the
grammar defines). The bracket syntax parses; the marker inside it does not
match any known annotation rule, so it reaches the model as
`ContentAnnotation::Unknown` and is reported as a validity question rather
than a parse failure.

`hello [@ xyz] world .` is invalid because `@` is not a defined scoped
annotation marker (word-level `@` suffixes like `@b`/`@c` are a completely
different construct, a word-suffix rather than a bracketed annotation; see E203).

## Expected Behavior

Exactly one E207 per unrecognized marker occurrence, reported once even when
the annotation attaches to a replaced word (`dog [: cat] [@ xyz] .`); this
was a real duplicate-emission bug (two independent emitters producing the
same diagnostic with different spans) fixed 2026-08-27 by consolidating to
one traversal-based emitter; see
`talkbank-model/src/model/annotation/scoped/types.rs` for the single
message-construction owner.

## CHAT Rule

<https://talkbank.org/0info/manuals/CHAT.html#Scoped_Symbols> lists the closed
set of defined scoped-annotation markers. A marker outside that set is not
valid CHAT; chatter reports it as E207 rather than silently accepting it or
downgrading the whole utterance to an unparsable-content error (E321/E316).

## Notes

- Do not confuse this with E203 (invalid `@` word-suffix form marker):
  E207 is about the content of a `[...]` bracketed annotation; E203 is about
  a word's own `@`-suffix.
- A malformed bracket (e.g. an unterminated `[`) is a different failure mode
  entirely and produces a parse-level diagnostic, not E207; E207 requires
  the bracket structure itself to parse successfully.
