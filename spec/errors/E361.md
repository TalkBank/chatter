+++
code = 'E361'
name = 'Invalid bullet timestamp'

[[example]]
title = 'A bullet whose times are not numbers'
level = 'utterance'
claim = { subsumed_by = 'E316' }
notes = """
The bullet grammar admits only digits around the underscore, so a
non-numeric bullet never parses as a bullet: tree-sitter leaves it as an
ERROR node and recovery reports E316. No code path produces E361 today
(`InvalidTimestamp` has no producer in the parser or the model), which is why
the registry lists it as not implemented. The earlier example (`[^ bad[`)
was not a timestamp at all.
"""
chat = """
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Media:	sample, audio
*CHI:	hello . \u0015abc_def\u0015
@End
"""

[[example]]
title = 'A numeric bullet'
level = 'utterance'
claim = 'legal'
chat = """
@UTF8
@Begin
@Languages:	eng
@Participants:	CHI Target_Child
@ID:	eng|corpus|CHI|||||Target_Child|||
@Media:	sample, audio
*CHI:	hello . \u0015500_1000\u0015
@End
"""
+++

**Status:** Current
**Last updated:** 2026-09-03 18:06 EDT

## Description

A media bullet `\u0015START_END\u0015` whose start or end is not a
millisecond count.

## Expected Behavior

Such a bullet is not a bullet to the grammar and is reported as E316
(unparsable content). E361 is registered for a value-level timestamp error
but nothing produces it: a bullet that parses always has two integers, and
the relation between them is E362's business. A numeric bullet is not
reported.

**Trigger**: See example above

## CHAT Rule

The CHAT manual's "Time Alignment" scope defines the bullet as two
millisecond counts separated by an underscore, between two U+0015
delimiters, after the terminator and any postcodes:
<https://talkbank.org/0info/manuals/CHAT.html#TimeAlignment_Scope>,
<https://talkbank.org/0info/manuals/CHAT.html#Utterance_Media>.

## Notes

- Retire or implement: either the grammar keeps refusing non-numeric bullets
  (then E361 has no reachable meaning and should be retired) or a lenient
  bullet token is added so the value can be reported precisely. The spec
  records the current state and takes no side.
