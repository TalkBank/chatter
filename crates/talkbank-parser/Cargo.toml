[package]
name = "talkbank-parser"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Canonical tree-sitter parser for TalkBank CHAT"
readme = "README.md"

[dependencies]
indexmap = { workspace = true }
smallvec = { workspace = true }
smol_str = { workspace = true }
talkbank-model = { workspace = true }
thiserror = { workspace = true } #unified
tracing = { workspace = true }
tree-sitter = { workspace = true }
tree-sitter-talkbank = { workspace = true }

[dev-dependencies]
# Path-only (no version) ON PURPOSE: talkbank-parser-tests is never
# published, so a versioned dev-dep would block `cargo publish`; cargo
# strips version-less dev-deps from the published manifest. Gated by
# scripts/release/check-foundation-publication-readiness.sh.
talkbank-parser-tests = { path = "../talkbank-parser-tests" }
divan = { workspace = true }
insta = { workspace = true }
proptest = { workspace = true }
rstest = { workspace = true }

[[bench]]
name = "parse"
harness = false

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]

# Panic-site audit completed 2026-04-29, zero non-test sites in
# this crate (see `docs/panic-audit/talkbank-parser.md`). `deny`
# prevents new sites without a deliberate `#[allow]` opt-in.

[lints]
workspace = true
