// @generated by `gen_form_markers` (spec/tools) from
// spec/form_markers/form_marker_registry.json. DO NOT EDIT MANUALLY.
//
// Regenerate with `just form-markers-gen`.

//! The closed set of CHAT special-form markers, derived from the registry.
//!
//! Support types that do not depend on the registry (`FormMarkerPayload`,
//! `UndeclaredFormMarker`, the `WriteChat` impl) are hand-written and live in
//! `crate::model::content::word::form`.

use crate::model::content::word::form::FormMarkerPayload;
use crate::model::content::word::form::UndeclaredFormMarker;
use schemars::JsonSchema;
use serde::Deserialize;
use serde::Serialize;
use std::borrow::Cow;
use talkbank_derive::SemanticEq;
use talkbank_derive::SpanShift;

/// Special-form suffix marker attached to a word token.
///
/// A word may carry one `@` marker naming what kind of form it is: a
/// child-invented word, a letter, a sung passage. The set is closed, and
/// declared once in `spec/form_markers/form_marker_registry.json`.
///
/// # Standard Markers
///
/// - `@b` - Babbling (`abame@b`)
/// - `@c` - Child-invented form (`gumma@c`, meaning sticky)
/// - `@d` - Dialect form (`younz@d`, meaning you)
/// - `@f` - Family-specific form (`bunko@f`, meaning broken)
/// - `@fp` - Filled pause (`um@fp`, deprecated, use `&-um` instead, because filled pauses are excluded from grammatical analysis)
/// - `@g` - General special form (`gongga@g`)
/// - `@i` - Interjection, interaction (`uhhuh@i`)
/// - `@k` - Multiple letters (`abcd@k`, mnemonic is "kana": a Japanese kana is one symbol for a whole syllable)
/// - `@l` - Letter (`b@l`, the letter b)
/// - `@ls` - Letter plural (`p@ls`, the plural of a letter)
/// - `@n` - Neologism (`breaked@n`, meaning broke)
/// - `@o` - Onomatopoeia (`woofwoof@o`, a dog barking)
/// - `@p` - Phonologically consistent form (`aga@p`)
/// - `@q` - Metalinguistic use (`if@q`, as in `no if@q-s or but@q-s`, when citing words)
/// - `@sas` - Sign and speech (`apple@sas`, signs and says apple)
/// - `@si` - Singing (`lalala@si`)
/// - `@sl` - Signed language (`apple@sl`, signs apple)
/// - `@t` - Test word (`wug@t`)
/// - `@u` - Unibet transcription (`binga@u`)
/// - `@wp` - Word play (`goobarumba@wp`)
/// - `@x` - Excluded words (`stuff@x`)
/// - `@z:<label>` - User-defined code (`word@z:rtfd`, any user code)
///
/// # References
///
/// - [Special Form Markers](https://talkbank.org/0info/manuals/CHAT.html#SpecialForm_Marker)
///
/// Per-marker anchors are on each variant below.
#[derive(
    Clone, Debug, PartialEq, Eq, Serialize, Deserialize, JsonSchema, SemanticEq, SpanShift,
)]
pub enum FormType {
    /// `@b` - Babbling (`abame@b`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Babbling_Marker>
    #[serde(rename = "b")]
    B,

    /// `@c` - Child-invented form (`gumma@c`, meaning sticky)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#ChildInvented_Marker>
    #[serde(rename = "c")]
    C,

    /// `@d` - Dialect form (`younz@d`, meaning you)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#DialectForm_Marker>
    #[serde(rename = "d")]
    D,

    /// `@f` - Family-specific form (`bunko@f`, meaning broken)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#FamilySpecificForm_Marker>
    #[serde(rename = "f")]
    F,

    /// `@fp` - Filled pause (`um@fp`, deprecated, use `&-um` instead, because filled pauses are excluded from grammatical analysis)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#FilledPause_Marker>
    #[serde(rename = "fp")]
    FP,

    /// `@g` - General special form (`gongga@g`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#GeneralSpecialForm_Marker>
    #[serde(rename = "g")]
    G,

    /// `@i` - Interjection, interaction (`uhhuh@i`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Interjection_Marker>
    #[serde(rename = "i")]
    I,

    /// `@k` - Multiple letters (`abcd@k`, mnemonic is "kana": a Japanese kana is one symbol for a whole syllable)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Kana_Marker>
    #[serde(rename = "k")]
    K,

    /// `@l` - Letter (`b@l`, the letter b)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Letter_Marker>
    #[serde(rename = "l")]
    L,

    /// `@ls` - Letter plural (`p@ls`, the plural of a letter)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Letter_Plural_Marker>
    #[serde(rename = "ls")]
    LS,

    /// `@n` - Neologism (`breaked@n`, meaning broke)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Neologism_Marker>
    #[serde(rename = "n")]
    N,

    /// `@o` - Onomatopoeia (`woofwoof@o`, a dog barking)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Onomatopoeia_Marker>
    #[serde(rename = "o")]
    O,

    /// `@p` - Phonologically consistent form (`aga@p`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#PCF_Marker>
    #[serde(rename = "p")]
    P,

    /// `@q` - Metalinguistic use (`if@q`, as in `no if@q-s or but@q-s`, when citing words)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#MetalinguisticReference_Marker>
    #[serde(rename = "q")]
    Q,

    /// `@sas` - Sign and speech (`apple@sas`, signs and says apple)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#SignAndSpeech_Marker>
    #[serde(rename = "sas")]
    SAS,

    /// `@si` - Singing (`lalala@si`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Singing_Marker>
    #[serde(rename = "si")]
    SI,

    /// `@sl` - Signed language (`apple@sl`, signs apple)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#SignLanguage_Marker>
    #[serde(rename = "sl")]
    SL,

    /// `@t` - Test word (`wug@t`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#TestWord_Marker>
    #[serde(rename = "t")]
    T,

    /// `@u` - Unibet transcription (`binga@u`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Unibet_Marker>
    #[serde(rename = "u")]
    U,

    /// `@wp` - Word play (`goobarumba@wp`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#WordPlay_Marker>
    #[serde(rename = "wp")]
    WP,

    /// `@x` - Excluded words (`stuff@x`)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#Excluded_Marker>
    #[serde(rename = "x")]
    X,

    /// `@z:<label>` - User-defined code (`word@z:rtfd`, any user code)
    ///
    /// Reference: <https://talkbank.org/0info/manuals/CHAT.html#User_Special_Form>
    #[serde(rename = "z")]
    UserDefined(String),

    /// Text that was written in a word's `@` slot but that no registry row
    /// declares (`word@zz`, `word@x:foo`).
    ///
    /// NOT a marker: it is a record of what the transcript actually said, kept
    /// so that recovery does not have to invent a marker that IS declared. The
    /// tree-sitter parser reports E203 and stores this; the payload is the
    /// text after the `@`, verbatim, so serializing the word reproduces the
    /// input exactly.
    ///
    /// Before this variant existed, that recovery path stored
    /// `UserDefined(payload)`, which claimed `word@zz` was the `@z`
    /// user-defined marker with label `zz`, and `to_chat_marker` rendered it
    /// back as `word@z:zz`. It was unobservable only because every command
    /// that serializes aborts on E203 first, which is a latent corruption
    /// rather than a safe one. The re2c parser stored nothing at all, so the
    /// two parsers disagreed on the same input.
    ///
    /// [`FormType::from_payload`] never returns this: it is reachable only
    /// from a parser that has already reported the marker undeclared.
    #[serde(rename = "undeclared")]
    Undeclared(String),
}

impl FormType {
    /// The list of declared markers, as shown to a user who wrote one that
    /// is not declared. Generated, so retiring a marker cannot leave it
    /// advertised in a diagnostic.
    pub const DECLARED_MARKERS_SUGGESTION: &'static str = "Valid form markers: @b @c @d @f @fp @g @i @k @l @ls @n @o @p @q @sas @si @sl @t @u @wp @x @z:<label>";

    /// Read the text after a word's `@` into a declared marker.
    ///
    /// # Case handling, which is inherited and INCOHERENT
    ///
    /// Label-free codes are matched case-insensitively, so `word@B` parses as
    /// [`FormType::B`], while a label-taking code is matched exactly, so
    /// `word@Z:grm` is undeclared. Both behaviours are carried over verbatim
    /// from the three hand-written sites this replaces, where the first came
    /// from a `to_lowercase()` and the second from a literal `"z:"` prefix
    /// test in each caller. Neither is defensible and the corpus authority
    /// writes every marker lowercase, but changing what chatter accepts is not
    /// a de-duplication and needs a comparison over real corpora, so the incoherence
    /// is preserved here, in one place where it can be seen, instead of in
    /// three where it could not.
    ///
    /// The case fold is ASCII, not Unicode, and allocates only when the input
    /// actually carries an uppercase byte. Every marker code is ASCII by
    /// construction, so the Unicode fold could never match anything the ASCII
    /// one misses, and corpus markers are already lowercase in essentially
    /// every case: this runs about a million times per corpus pass, once per
    /// form-marked word, and previously allocated a `String` every time.
    pub fn from_payload(payload: FormMarkerPayload<'_>) -> Result<Self, UndeclaredFormMarker> {
        let declared = match payload.label() {
            None => {
                let code = payload.code();
                let folded;
                let code = if code.bytes().any(|b| b.is_ascii_uppercase()) {
                    folded = code.to_ascii_lowercase();
                    folded.as_str()
                } else {
                    code
                };
                match code {
                    "b" => Some(FormType::B),
                    "c" => Some(FormType::C),
                    "d" => Some(FormType::D),
                    "f" => Some(FormType::F),
                    "fp" => Some(FormType::FP),
                    "g" => Some(FormType::G),
                    "i" => Some(FormType::I),
                    "k" => Some(FormType::K),
                    "l" => Some(FormType::L),
                    "ls" => Some(FormType::LS),
                    "n" => Some(FormType::N),
                    "o" => Some(FormType::O),
                    "p" => Some(FormType::P),
                    "q" => Some(FormType::Q),
                    "sas" => Some(FormType::SAS),
                    "si" => Some(FormType::SI),
                    "sl" => Some(FormType::SL),
                    "t" => Some(FormType::T),
                    "u" => Some(FormType::U),
                    "wp" => Some(FormType::WP),
                    "x" => Some(FormType::X),
                    _ => None,
                }
            }
            // An empty label is unreachable through either parser (both require
            // at least one character after the colon) and is refused here so it
            // cannot be reached another way.
            Some(label) if !label.is_empty() => match payload.code() {
                "z" => Some(FormType::UserDefined(label.to_owned())),
                _ => None,
            },
            Some(_) => None,
        };
        declared.ok_or_else(|| UndeclaredFormMarker::new(payload))
    }

    /// The marker payload written after `@` in CHAT output.
    ///
    /// The exact inverse of [`FormType::from_payload`], because both are
    /// generated from the same registry rows: a marker cannot be readable and
    /// unwritable, or written under a code nothing parses.
    pub fn to_chat_marker(&self) -> Cow<'static, str> {
        match self {
            FormType::B => Cow::Borrowed("b"),
            FormType::C => Cow::Borrowed("c"),
            FormType::D => Cow::Borrowed("d"),
            FormType::F => Cow::Borrowed("f"),
            FormType::FP => Cow::Borrowed("fp"),
            FormType::G => Cow::Borrowed("g"),
            FormType::I => Cow::Borrowed("i"),
            FormType::K => Cow::Borrowed("k"),
            FormType::L => Cow::Borrowed("l"),
            FormType::LS => Cow::Borrowed("ls"),
            FormType::N => Cow::Borrowed("n"),
            FormType::O => Cow::Borrowed("o"),
            FormType::P => Cow::Borrowed("p"),
            FormType::Q => Cow::Borrowed("q"),
            FormType::SAS => Cow::Borrowed("sas"),
            FormType::SI => Cow::Borrowed("si"),
            FormType::SL => Cow::Borrowed("sl"),
            FormType::T => Cow::Borrowed("t"),
            FormType::U => Cow::Borrowed("u"),
            FormType::WP => Cow::Borrowed("wp"),
            FormType::X => Cow::Borrowed("x"),
            FormType::UserDefined(label) => Cow::Owned(format!("z:{label}")),
            // Verbatim, so a word carrying an undeclared marker serializes
            // back to exactly what was read.
            FormType::Undeclared(text) => Cow::Owned(text.clone()),
        }
    }
}
